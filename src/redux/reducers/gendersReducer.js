const initialState = {
    data : {},
    loading : false,
    error : null
}

export default function gendersReducer (state = initialState, action) {
    switch(action.type){
        default: return state
    }
}
